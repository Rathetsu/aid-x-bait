import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const AidxbaitStore = () => {
	return (
		<SafeAreaView>
			<Text>AidxBait Store</Text>
		</SafeAreaView>
	);
};

export default AidxbaitStore;
