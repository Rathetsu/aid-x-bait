import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const onlineConsultation = () => {
	return (
		<SafeAreaView>
			<Text>onlineConsultation</Text>
		</SafeAreaView>
	);
};

export default onlineConsultation;
